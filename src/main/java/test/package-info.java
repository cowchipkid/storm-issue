/**
 * Test bolts and spouts live here. The TestParallelismToplology builds a very simple
 * topo that just works on data as fast as it can. This is used to test effectiveness
 * of distribution across nodes, either with or without using anchored tuples. As of 2.2.0, 
 * anchored tuples seem to refuse distribution across nodes.<p>
 * 
 */
package test;